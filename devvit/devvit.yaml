name: cassandra-world
version: 0.0.1
slug: cassandra-world

# Devvit app: The Cabin Inside Reddit
# A Reddit post that IS the glass cabin — users talk to Cassandra directly from Reddit.
# Architecture: webview (UI) ↔ postMessage ↔ Devvit backend (TS) ↔ Echoes API (Cloud Run)
